# Hookify Rules for {PROJECT_TITLE}

> Generated by buddyx-forge. These rules work with the hookify plugin to prevent unwanted behaviors.
> Edit or add rules with `/hookify` command.

## Rule: Never commit without permission
- trigger: Bash command containing `git commit` or `git push`
- action: block
- message: "Git commits are manual. Use /commit when ready."

{FRAMEWORK_HOOKIFY_RULES}
